apiVersion: v1
kind: Service
metadata:
  name: claude-transcript-viewer-frontend
  labels:
    app: claude-transcript-viewer-frontend
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: claude-transcript-viewer-frontend
    tier: frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
