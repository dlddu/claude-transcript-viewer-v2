apiVersion: v1
kind: Service
metadata:
  name: claude-transcript-viewer
spec:
  selector:
    app: claude-transcript-viewer
  ports:
    - name: api
      protocol: TCP
      port: 3000
      targetPort: 3000
    - name: web
      protocol: TCP
      port: 80
      targetPort: 80
