apiVersion: v1
kind: Service
metadata:
  name: claude-transcript-viewer
  labels:
    app: claude-transcript-viewer
spec:
  type: ClusterIP
  selector:
    app: claude-transcript-viewer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
